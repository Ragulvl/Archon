services:
  - type: web
    name: archon
    runtime: node
    buildCommand: npm run install:all && npm run prisma:generate && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: GROQ_API_KEYS
        sync: false
