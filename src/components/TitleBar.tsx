const TitleBar = () => {
  return (
    <div className="flex items-center h-10 bg-surface-1 border-b border-border px-4 select-none">
      <span className="text-sm font-semibold text-foreground">Archon</span>
    </div>
  );
};

export default TitleBar;
